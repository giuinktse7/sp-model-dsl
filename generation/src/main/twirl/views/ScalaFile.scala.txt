@(packageName: String, xs: Seq[String], dependencies: Seq[String])
@if(packageName.nonEmpty) { package @packageName }

@import views.txt.util
@for(d <- dependencies if d.nonEmpty) {
     @d.replaceAll("\n", "")
   }

@for(x <- xs if x.nonEmpty) {
 @x
}