@(className: String, xs: Seq[codegen.definition.FieldDefinition])

import monocle.macros.Lenses

@@Lenses case class @(className)(
  @for((x, index) <- xs.zipWithIndex) {
    @x.name: @x.className @if(index < xs.size - 1) { , }
  }
)